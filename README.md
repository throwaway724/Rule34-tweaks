# Rule34-tweaks
This is a GreaseMonkey script to add some extra functionality to rule34.xxx and save your settings and favorites without an account or cookies. It replaces the "my account" page with a custom settings page that it stores the values of locally. Settings to blacklist tags via regular expressions and customise the theme were added.
